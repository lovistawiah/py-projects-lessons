# Array methods
"""
Insert: add an element to the start of an array O(n)
Append: add an element to the end of the array
len(): to get the size of an array
extend(): add an array to another array  O(k)
delete: remove the element of an array O(n)
"""

new_list = [1, 2, 3]
# accessing the index of an array
result = new_list[0]
print(result)

# searching index of an array
new_Array = [1, 2, 3, 4, 5, 6, 7]
for n in new_Array:
    if n == 1:
        print(True)
    break

#inserting in an array is linear runtime

